<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome - Gemini Bank</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        /* GLOBAL & LANDING PAGE STYLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            overflow: hidden;
        }

        /* --- NEW QUANTUM VAULT STYLES --- */
        
        /* MAIN VAULT CONTAINER */
        .vault-scene {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: #000;
            display: flex;
            align-items: center;
            justify-content: center;
            perspective: 3000px;
            transform-style: preserve-3d;
            z-index: 99999;
        }

        /* DEEP SPACE BACKGROUND */
        .deep-space {
            position: absolute;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(ellipse at 20% 30%, #1a0033 0%, transparent 40%),
                radial-gradient(ellipse at 60% 70%, #001a33 0%, transparent 45%),
                radial-gradient(ellipse at 80% 10%, #0a001a 0%, transparent 50%),
                radial-gradient(circle at center, #000511 0%, #000 70%);
            animation: spaceRotate 60s linear infinite;
        }

        @keyframes spaceRotate {
            0% { transform: rotate(0deg) scale(1.1); }
            100% { transform: rotate(360deg) scale(1.1); }
        }

        /* NEBULA CLOUDS */
        .nebula-layer {
            position: absolute;
            width: 150%;
            height: 150%;
            top: -25%;
            left: -25%;
            opacity: 0.4;
            mix-blend-mode: screen;
            filter: blur(40px);
            background: 
                radial-gradient(circle at 20% 50%, rgba(138, 43, 226, 0.4) 0%, transparent 50%),
                radial-gradient(circle at 80% 50%, rgba(30, 144, 255, 0.4) 0%, transparent 50%),
                radial-gradient(circle at 50% 20%, rgba(255, 20, 147, 0.3) 0%, transparent 50%);
            animation: nebulaDrift 30s ease-in-out infinite alternate;
        }

        @keyframes nebulaDrift {
            0% { transform: translate(0, 0) rotate(0deg); }
            100% { transform: translate(-5%, 5%) rotate(5deg); }
        }

        /* STELLAR GRID */
        .stellar-grid {
            position: absolute;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(rgba(0, 255, 255, 0.01) 0.5px, transparent 0.5px),
                linear-gradient(90deg, rgba(0, 255, 255, 0.01) 0.5px, transparent 0.5px);
            background-size: 100px 100px, 100px 100px, 20px 20px, 20px 20px;
            animation: gridPerspective 20s ease-in-out infinite;
            transform: rotateX(60deg) translateZ(-100px);
            transform-origin: center center;
        }

        @keyframes gridPerspective {
            0%, 100% { transform: rotateX(60deg) translateZ(-100px) scale(1); }
            50% { transform: rotateX(60deg) translateZ(-50px) scale(1.1); }
        }

        /* QUANTUM PARTICLES SYSTEM */
        .quantum-field {
            position: absolute;
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
        }

        .quantum-particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: #fff;
            border-radius: 50%;
            box-shadow: 
                0 0 10px currentColor,
                0 0 20px currentColor,
                0 0 30px currentColor;
            animation: quantumOrbit 20s linear infinite;
            transform-style: preserve-3d;
        }

        @keyframes quantumOrbit {
            0% { transform: rotateY(0deg) rotateX(0deg) translateZ(300px); }
            100% { transform: rotateY(360deg) rotateX(360deg) translateZ(300px); }
        }

        /* ENERGY RINGS */
        .energy-ring {
            position: absolute;
            border: 2px solid rgba(0, 255, 255, 0.3);
            border-radius: 50%;
            animation: ringPulse 4s ease-in-out infinite;
        }
        .energy-ring:nth-of-type(1) { width: 600px; height: 600px; animation-delay: 0s; border-color: rgba(0, 255, 255, 0.2); }
        .energy-ring:nth-of-type(2) { width: 500px; height: 500px; animation-delay: 0.5s; border-color: rgba(138, 43, 226, 0.2); }
        .energy-ring:nth-of-type(3) { width: 400px; height: 400px; animation-delay: 1s; border-color: rgba(255, 20, 147, 0.2); }

        @keyframes ringPulse {
            0%, 100% { transform: scale(1) rotateZ(0deg); opacity: 0.3; }
            50% { transform: scale(1.2) rotateZ(180deg); opacity: 0.8; }
        }

        /* MAIN VAULT STRUCTURE */
        .vault-chamber {
            position: relative;
            width: 500px;
            height: 500px;
            transform-style: preserve-3d;
            /* MODIFIED: Changed animation to the new, smoother materialization effect */
            animation: vaultMaterialize 2.5s cubic-bezier(0.23, 1, 0.32, 1);
        }

        /* MODIFIED: Replaced old 'vaultInitialize' with new 'vaultMaterialize' keyframes for a smoother entrance */
        @keyframes vaultMaterialize {
            0% {
                transform: translateZ(-3000px) scale(0.1);
                opacity: 0;
                filter: blur(20px);
            }
            70% {
                transform: translateZ(50px) scale(1.05);
                opacity: 1;
                filter: blur(2px);
            }
            100% {
                transform: translateZ(0) scale(1);
                opacity: 1;
                filter: blur(0);
            }
        }

        /* VAULT FRAME */
        .vault-frame {
            position: absolute;
            width: 100%; height: 100%;
            border: 4px solid;
            border-image: linear-gradient(45deg, #00ffff, #ff00ff, #00ffff) 1;
            border-radius: 30px;
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.8) 0%, rgba(0, 20, 40, 0.9) 100%);
            box-shadow: inset 0 0 100px rgba(0, 255, 255, 0.2), 0 0 200px rgba(0, 255, 255, 0.3), 0 0 300px rgba(138, 43, 226, 0.2);
            animation: framePulse 3s ease-in-out infinite;
        }

        @keyframes framePulse {
            0%, 100% { box-shadow: inset 0 0 100px rgba(0, 255, 255, 0.2), 0 0 200px rgba(0, 255, 255, 0.3); }
            50% { box-shadow: inset 0 0 150px rgba(138, 43, 226, 0.3), 0 0 300px rgba(138, 43, 226, 0.4); }
        }

        /* VAULT DOOR */
        .vault-door {
            position: absolute;
            width: 100%; height: 100%;
            border-radius: 25px;
            background: radial-gradient(circle at 30% 30%, rgba(138, 43, 226, 0.2) 0%, transparent 60%), linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            transform-origin: left center;
            transition: transform 3s cubic-bezier(0.645, 0.045, 0.355, 1);
            box-shadow: inset 0 0 100px rgba(0, 0, 0, 0.5), 0 20px 60px rgba(0, 0, 0, 0.5);
            overflow: hidden;
        }
        .vault-door.opening { transform: perspective(1000px) rotateY(-120deg); }

        .door-tech-overlay {
            position: absolute;
            width: 100%; height: 100%;
            opacity: 0.5;
            background: repeating-linear-gradient(0deg, transparent 0, rgba(0, 255, 255, 0.1) 1px, transparent 2px, transparent 20px), repeating-linear-gradient(90deg, transparent 0, rgba(0, 255, 255, 0.1) 1px, transparent 2px, transparent 20px);
            animation: techScan 10s linear infinite;
        }

        @keyframes techScan {
            0% { transform: translate(0, 0); }
            100% { transform: translate(20px, 20px); }
        }

        /* CENTRAL LOCK MECHANISM */
        .lock-mechanism {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 300px; height: 300px;
        }

        .lock-ring {
            position: absolute;
            width: 100%; height: 100%;
            border: 3px solid; border-radius: 50%;
            border-color: transparent;
            border-top-color: rgba(0, 255, 255, 0.8);
            border-right-color: rgba(138, 43, 226, 0.8);
            animation: lockRotate 3s linear infinite;
        }
        .lock-ring:nth-child(1) { width: 100%; height: 100%; animation-direction: normal; animation-duration: 3s; }
        .lock-ring:nth-child(2) { width: 80%; height: 80%; top: 10%; left: 10%; animation-direction: reverse; animation-duration: 2.5s; border-top-color: rgba(255, 20, 147, 0.8); border-right-color: rgba(30, 144, 255, 0.8); }
        .lock-ring:nth-child(3) { width: 60%; height: 60%; top: 20%; left: 20%; animation-direction: normal; animation-duration: 2s; border-top-color: rgba(255, 215, 0, 0.8); border-right-color: rgba(0, 255, 127, 0.8); }
        .lock-ring.unlocking { animation: unlockSpin 2s cubic-bezier(0.68, -0.55, 0.265, 1.55) forwards; }

        @keyframes lockRotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        @keyframes unlockSpin {
            0% { transform: rotate(0deg) scale(1); }
            50% { transform: rotate(720deg) scale(1.2); }
            100% { transform: rotate(1440deg) scale(0); opacity: 0; }
        }

        /* CORE ENERGY ORB */
        .energy-core {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 100px; height: 100px;
            border-radius: 50%;
            background: radial-gradient(circle at 30% 30%, #ffffff 0%, transparent 50%), radial-gradient(circle at center, #00ffff 0%, #0080ff 50%, #004080 100%);
            box-shadow: 0 0 50px #00ffff, 0 0 100px #0080ff, 0 0 150px #004080, inset 0 0 50px rgba(255, 255, 255, 0.5);
            animation: corePulse 2s ease-in-out infinite;
        }
        .energy-core.charged { animation: coreCharge 2s ease-out forwards; }
        
        @keyframes corePulse {
            0%, 100% { transform: translate(-50%, -50%) scale(1); box-shadow: 0 0 50px #00ffff, 0 0 100px #0080ff, 0 0 150px #004080; }
            50% { transform: translate(-50%, -50%) scale(1.1); box-shadow: 0 0 80px #00ffff, 0 0 160px #0080ff, 0 0 240px #004080; }
        }
        @keyframes coreCharge {
            0% { transform: translate(-50%, -50%) scale(1); }
            50% { transform: translate(-50%, -50%) scale(2); box-shadow: 0 0 100px #fff, 0 0 200px #00ffff, 0 0 300px #0080ff; }
            100% { transform: translate(-50%, -50%) scale(0); opacity: 0; }
        }

        /* SECURITY SCANNER */
        .scanner-beam {
            position: absolute;
            width: 100%; height: 4px;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.8), rgba(255, 255, 255, 1), rgba(0, 255, 255, 0.8), transparent);
            top: -10px; opacity: 0;
            box-shadow: 0 0 20px rgba(0, 255, 255, 1), 0 0 40px rgba(0, 255, 255, 0.8);
        }
        .scanner-beam.scanning { animation: scanSequence 3s ease-in-out; }

        @keyframes scanSequence {
            0% { top: -10px; opacity: 0; }
            10% { opacity: 1; }
            50% { top: 100%; }
            90% { opacity: 1; }
            100% { top: 110%; opacity: 0; }
        }

        /* STATUS DISPLAY */
        .status-display {
            position: absolute;
            top: 50px; left: 50%;
            transform: translateX(-50%);
            text-align: center; color: #00ffff;
            font-size: 24px; font-weight: 300;
            letter-spacing: 4px; text-transform: uppercase;
            text-shadow: 0 0 20px currentColor, 0 0 40px currentColor;
            opacity: 0;
            animation: statusFadeIn 1s ease-out forwards;
            animation-delay: 0.5s;
        }

        @keyframes statusFadeIn { to { opacity: 1; } }

        /* AUTHENTICATION NODES */
        .auth-nodes {
            position: absolute;
            bottom: 80px; left: 50%;
            transform: translateX(-50%);
            display: flex; gap: 30px;
        }
        .auth-node {
            width: 30px; height: 30px;
            border: 2px solid rgba(0, 255, 255, 0.3);
            background: rgba(0, 20, 40, 0.8);
            border-radius: 50%; position: relative;
            transition: all 0.5s ease;
        }
        .auth-node::before {
            content: ''; position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 10px; height: 10px;
            background: rgba(0, 255, 255, 0.3);
            border-radius: 50%;
            transition: all 0.5s ease;
        }
        .auth-node.authenticated {
            border-color: #00ffff;
            background: rgba(0, 255, 255, 0.1);
            box-shadow: 0 0 20px #00ffff, inset 0 0 20px rgba(0, 255, 255, 0.3);
            animation: nodeSuccess 0.5s ease;
        }
        .auth-node.authenticated::before {
            width: 20px; height: 20px;
            background: #00ffff; box-shadow: 0 0 20px #00ffff;
        }

        @keyframes nodeSuccess {
            0% { transform: scale(1); }
            50% { transform: scale(1.3); }
            100% { transform: scale(1); }
        }

        /* PROGRESS INDICATOR */
        .progress-bar-container { /* Renamed from .progress-ring to avoid conflict */
            position: absolute;
            bottom: 150px; left: 50%;
            transform: translateX(-50%);
            width: 400px; height: 10px;
        }
        .progress-track {
            width: 100%; height: 100%;
            background: rgba(0, 255, 255, 0.1);
            border-radius: 5px; overflow: hidden;
            border: 1px solid rgba(0, 255, 255, 0.2);
        }
        .progress-fill-vault { /* Renamed from .progress-fill */
            height: 100%;
            background: linear-gradient(90deg, #00ffff 0%, #0080ff 25%, #8a2be2 50%, #ff1493 75%, #00ffff 100%);
            background-size: 200% 100%;
            width: 0%;
            transition: width 0.5s ease;
            animation: progressGradient 3s linear infinite;
            box-shadow: 0 0 20px currentColor;
        }
        @keyframes progressGradient {
            0% { background-position: 0% 0%; }
            100% { background-position: 200% 0%; }
        }

        /* PLASMA EFFECTS */
        .plasma-field {
            position: absolute;
            width: 100%; height: 100%;
            filter: blur(60px); opacity: 0.6;
            mix-blend-mode: screen; pointer-events: none;
        }
        .plasma-blob { position: absolute; border-radius: 50%; animation: plasmaFloat 20s infinite ease-in-out; }
        .plasma-blob:nth-child(1) { width: 300px; height: 300px; background: radial-gradient(circle, rgba(0, 255, 255, 0.8) 0%, transparent 70%); top: 10%; left: 10%; animation-duration: 15s; }
        .plasma-blob:nth-child(2) { width: 250px; height: 250px; background: radial-gradient(circle, rgba(138, 43, 226, 0.8) 0%, transparent 70%); bottom: 10%; right: 10%; animation-duration: 18s; animation-delay: -5s; }
        .plasma-blob:nth-child(3) { width: 200px; height: 200px; background: radial-gradient(circle, rgba(255, 20, 147, 0.8) 0%, transparent 70%); top: 50%; left: 50%; animation-duration: 20s; animation-delay: -10s; }

        @keyframes plasmaFloat {
            0%, 100% { transform: translate(0, 0) scale(1); }
            25% { transform: translate(100px, -50px) scale(1.1); }
            50% { transform: translate(-50px, 100px) scale(0.9); }
            75% { transform: translate(-100px, -100px) scale(1.05); }
        }

        /* COMPLETION FLASH */
        .completion-flash {
            position: absolute;
            width: 100%; height: 100%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.8) 0%, transparent 70%);
            opacity: 0; pointer-events: none;
        }
        .completion-flash.active { animation: flashBurst 1s ease-out; }
        @keyframes flashBurst {
            0% { opacity: 0; transform: scale(0.5); }
            50% { opacity: 1; transform: scale(1.2); }
            100% { opacity: 0; transform: scale(1.5); }
        }

        /* --- END OF NEW VAULT STYLES --- */


        /* MAIN LANDING PAGE STYLES (PRESERVED AS REQUESTED) */
        .main-content {
            opacity: 0;
            transform: scale(0.95);
            transition: all 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .main-content.visible {
            opacity: 1;
            transform: scale(1);
        }

        .hero-section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            padding: 6rem 0;
        }

        .hero-content {
            position: relative;
            z-index: 10;
            animation: fadeInUp 1s ease-out;
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-title {
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 900;
            background: linear-gradient(135deg, #ffffff 0%, #667eea 50%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 2rem;
            line-height: 1.1;
            text-shadow: 0 0 40px rgba(102, 126, 234, 0.3);
            animation: titleGlow 3s ease-in-out infinite alternate;
        }

        @keyframes titleGlow {
            0% { filter: drop-shadow(0 0 20px rgba(102, 126, 234, 0.3)); }
            100% { filter: drop-shadow(0 0 40px rgba(102, 126, 234, 0.6)); }
        }

        .hero-subtitle {
            font-size: 1.4rem; color: rgba(255, 255, 255, 0.85);
            font-weight: 300; line-height: 1.6; margin-bottom: 3rem;
            max-width: 600px; margin-left: auto; margin-right: auto;
        }

        .hero-buttons { display: flex; gap: 1.5rem; justify-content: center; flex-wrap: wrap; margin-top: 3rem; }

        .btn-premium {
            position: relative; padding: 1rem 2.5rem; font-size: 1.1rem;
            font-weight: 600; border-radius: 50px; border: none;
            text-decoration: none; transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            overflow: hidden; text-transform: uppercase; letter-spacing: 1px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }
        .btn-premium::before {
            content: ''; position: absolute; top: 0; left: -100%;
            width: 100%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        .btn-premium:hover::before { left: 100%; }
        .btn-primary-premium {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
        }
        .btn-primary-premium:hover { transform: translateY(-3px); box-shadow: 0 25px 50px rgba(102, 126, 234, 0.6); color: white; }
        .btn-secondary-premium {
            background: rgba(255, 255, 255, 0.1); color: white;
            border: 2px solid rgba(255, 255, 255, 0.3); backdrop-filter: blur(10px);
        }
        .btn-secondary-premium:hover {
            background: rgba(255, 255, 255, 0.2); border-color: rgba(255, 255, 255, 0.5);
            transform: translateY(-3px); color: white; box-shadow: 0 25px 50px rgba(255, 255, 255, 0.1);
        }

        /* FLOATING 3D ELEMENTS */
        .floating-3d { position: absolute; pointer-events: none; opacity: 0.6; }
        .floating-card {
            width: 80px; height: 50px;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.8), rgba(118, 75, 162, 0.8));
            border-radius: 8px; backdrop-filter: blur(10px);
            animation: float3D 8s ease-in-out infinite;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }
        .floating-coin {
            width: 40px; height: 40px;
            background: linear-gradient(135deg, #ffd700, #ffed4a);
            border-radius: 50%; animation: float3D 6s ease-in-out infinite reverse;
            box-shadow: 0 10px 30px rgba(255, 215, 0, 0.3);
        }
        @keyframes float3D {
            0%, 100% { transform: translateY(0px) rotateY(0deg) rotateX(0deg); }
            33% { transform: translateY(-30px) rotateY(120deg) rotateX(10deg); }
            66% { transform: translateY(-15px) rotateY(240deg) rotateX(-10deg); }
        }
        .floating-card:nth-child(1) { top: 20%; left: 10%; animation-delay: 0s; }
        .floating-card:nth-child(2) { top: 30%; right: 15%; animation-delay: 2s; }
        .floating-coin:nth-child(3) { bottom: 40%; left: 20%; animation-delay: 1s; }
        .floating-coin:nth-child(4) { top: 60%; right: 10%; animation-delay: 3s; }

        /* PARTICLE SYSTEM */
        .particles { position: absolute; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: 1; }
        .particle {
            position: absolute; width: 2px; height: 2px;
            background: rgba(102, 126, 234, 0.6); border-radius: 50%;
            animation: particleFloat 15s linear infinite;
        }
        @keyframes particleFloat {
            0% { opacity: 0; transform: translateY(100vh) translateX(0); }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { opacity: 0; transform: translateY(-10vh) translateX(100px); }
        }

        /* STATS CARDS */
        .stats-section { margin-top: 4rem; position: relative; z-index: 10; }
        .stat-card {
            background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px);
            border-radius: 20px; padding: 2rem; text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2); transition: all 0.4s ease;
            animation: statCardAppear 1s ease-out; animation-delay: 1.5s; animation-fill-mode: both;
        }
        @keyframes statCardAppear {
            from { opacity: 0; transform: translateY(30px) scale(0.9); }
            to { opacity: 1; transform: translateY(0) scale(1); }
        }
        .stat-card:hover {
            transform: translateY(-10px) scale(1.05);
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 25px 50px rgba(102, 126, 234, 0.2);
        }
        .stat-number {
            font-size: 3rem; font-weight: 800;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            background-clip: text; margin-bottom: 0.5rem;
        }
        .stat-label { color: rgba(255, 255, 255, 0.8); font-size: 1.1rem; font-weight: 500; }

        /* RESPONSIVE DESIGN FOR LANDING PAGE */
        @media (max-width: 768px) {
            .hero-section { padding: 4rem 0; }
            .hero-buttons { flex-direction: column; align-items: center; }
            .btn-premium { width: 100%; max-width: 300px; }
        }
    </style>
</head>
<body>

    <div class="vault-scene" id="vaultScene">
        <div class="deep-space"></div>
        <div class="nebula-layer"></div>
        <div class="stellar-grid"></div>
        <div class="plasma-field">
            <div class="plasma-blob"></div>
            <div class="plasma-blob"></div>
            <div class="plasma-blob"></div>
        </div>
        <div class="energy-ring"></div>
        <div class="energy-ring"></div>
        <div class="energy-ring"></div>
        <div class="quantum-field" id="quantumField"></div>
        <div class="status-display" id="statusDisplay">INITIALIZING QUANTUM VAULT</div>
        <div class="vault-chamber">
            <div class="vault-frame"></div>
            <div class="vault-door" id="vaultDoor">
                <div class="door-tech-overlay"></div>
                <div class="scanner-beam" id="scannerBeam"></div>
                <div class="lock-mechanism">
                    <div class="lock-ring" id="ring1"></div>
                    <div class="lock-ring" id="ring2"></div>
                    <div class="lock-ring" id="ring3"></div>
                    <div class="energy-core" id="energyCore"></div>
                </div>
            </div>
        </div>
        <div class="auth-nodes">
            <div class="auth-node" id="node1"></div>
            <div class="auth-node" id="node2"></div>
            <div class="auth-node" id="node3"></div>
            <div class="auth-node" id="node4"></div>
            <div class="auth-node" id="node5"></div>
        </div>
        <div class="progress-bar-container">
            <div class="progress-track">
                <div class="progress-fill-vault" id="progressFill"></div>
            </div>
        </div>
        <div class="completion-flash" id="completionFlash"></div>
    </div>
    
    <div class="main-content" id="mainContent">
        <div class="hero-section">
            <div class="particles">
                <div class="particle" style="left: 10%; animation-delay: 0s;"></div>
                <div class="particle" style="left: 20%; animation-delay: 3s;"></div>
                <div class="particle" style="left: 30%; animation-delay: 6s;"></div>
                <div class="particle" style="left: 40%; animation-delay: 9s;"></div>
                <div class="particle" style="left: 50%; animation-delay: 12s;"></div>
                <div class="particle" style="left: 60%; animation-delay: 2s;"></div>
                <div class="particle" style="left: 70%; animation-delay: 5s;"></div>
                <div class="particle" style="left: 80%; animation-delay: 8s;"></div>
                <div class="particle" style="left: 90%; animation-delay: 11s;"></div>
            </div>

            <div class="floating-3d floating-card"></div>
            <div class="floating-3d floating-card"></div>
            <div class="floating-3d floating-coin"></div>
            <div class="floating-3d floating-coin"></div>

            <div class="container">
                <div class="hero-content text-center">
                    <h1 class="hero-title">
                        <i class="fas fa-university me-3" style="color: rgba(102, 126, 234, 0.8);"></i>
                        Gemini Bank
                    </h1>
                    <p class="hero-subtitle">
                        Your secure and reliable partner in modern digital banking. Experience the future of finance with cutting-edge technology, unparalleled security, and personalized service that adapts to your needs.
                    </p>
                    <div class="hero-buttons">
                        <a href="/user/register" class="btn-premium btn-primary-premium">
                            <i class="fas fa-rocket me-2"></i>
                            Start Your Journey
                        </a>
                        <a href="/user/login" class="btn-premium btn-secondary-premium">
                            <i class="fas fa-sign-in-alt me-2"></i>
                            Access Account
                        </a>
                    </div>
                </div>
            </div>

            <div class="container stats-section">
                <div class="row g-4">
                    <div class="col-md-4">
                        <div class="stat-card">
                            <div class="stat-number">1M+</div>
                            <div class="stat-label">Trusted Customers</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-card">
                            <div class="stat-number">99%</div>
                            <div class="stat-label">Uptime Guarantee</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-card">
                            <div class="stat-number">24/7</div>
                            <div class="stat-label">Customer Support</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // FUNCTION TO SHOW THE MAIN LANDING PAGE (UNCHANGED)
        function showLandingPage() {
            const mainContent = document.getElementById('mainContent');
            mainContent.classList.add('visible');
            document.body.style.overflow = 'auto';
        }
        
        // NEW VAULT: Create quantum particles
        function initQuantumField() {
            const field = document.getElementById('quantumField');
            const colors = ['#00ffff', '#ff00ff', '#ffff00', '#00ff00', '#ff0080'];
            for (let i = 0; i < 30; i++) {
                const particle = document.createElement('div');
                particle.className = 'quantum-particle';
                particle.style.color = colors[i % colors.length];
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (15 + Math.random() * 10) + 's';
                field.appendChild(particle);
            }
        }

        // NEW VAULT: Vault opening sequence
        async function initVaultSequence() {
            const vaultScene = document.getElementById('vaultScene');
            const statusDisplay = document.getElementById('statusDisplay');
            const scannerBeam = document.getElementById('scannerBeam');
            const nodes = Array.from({ length: 5 }, (_, i) => document.getElementById(`node${i + 1}`));
            const progressFill = document.getElementById('progressFill');
            const rings = Array.from({ length: 3 }, (_, i) => document.getElementById(`ring${i + 1}`));
            const energyCore = document.getElementById('energyCore');
            const vaultDoor = document.getElementById('vaultDoor');
            const completionFlash = document.getElementById('completionFlash');

            const wait = ms => new Promise(resolve => setTimeout(resolve, ms));

            const sequence = [
                { delay: 2500, action: () => { statusDisplay.textContent = 'SCANNING BIOMETRICS'; scannerBeam.classList.add('scanning'); progressFill.style.width = '15%'; }},
                { delay: 1500, action: () => { nodes[0].classList.add('authenticated'); progressFill.style.width = '25%'; }},
                { delay: 500, action: () => { nodes[1].classList.add('authenticated'); progressFill.style.width = '40%'; }},
                { delay: 500, action: () => { statusDisplay.textContent = 'QUANTUM AUTHENTICATION'; nodes[2].classList.add('authenticated'); progressFill.style.width = '55%'; }},
                { delay: 500, action: () => { nodes[3].classList.add('authenticated'); progressFill.style.width = '70%'; }},
                { delay: 500, action: () => { nodes[4].classList.add('authenticated'); progressFill.style.width = '85%'; statusDisplay.textContent = 'DECRYPTING LOCK MATRIX'; }},
                { delay: 1000, action: () => { rings.forEach(ring => ring.classList.add('unlocking')); energyCore.classList.add('charged'); progressFill.style.width = '95%'; }},
                { delay: 1500, action: () => { statusDisplay.textContent = 'ACCESS GRANTED'; progressFill.style.width = '100%'; completionFlash.classList.add('active'); }},
                { delay: 500, action: () => { vaultDoor.classList.add('opening'); }},
                { 
                    delay: 2000, 
                    action: () => {
                        vaultScene.style.transition = 'opacity 1.5s ease-out';
                        vaultScene.style.opacity = '0';
                        showLandingPage();
                        
                        setTimeout(() => {
                            vaultScene.remove();
                        }, 1500);
                    }
                }
            ];

            for (const step of sequence) {
                await wait(step.delay);
                step.action();
            }
        }

        // INITIALIZATION SCRIPT (MODIFIED TO RUN THE NEW SEQUENCE)
        document.addEventListener('DOMContentLoaded', function() {
            initQuantumField();
            initVaultSequence();

            // ORIGINAL LANDING PAGE SCRIPTS
            setTimeout(() => {
                const observerOptions = { threshold: 0.1, rootMargin: '0px 0px -50px 0px' };
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.style.animationPlayState = 'running';
                        }
                    });
                }, observerOptions);
                document.querySelectorAll('.stat-card').forEach(card => observer.observe(card));

                document.querySelectorAll('.btn-premium').forEach(btn => {
                    btn.addEventListener('mouseenter', function() { this.style.transform = 'translateY(-3px) scale(1.02)'; });
                    btn.addEventListener('mouseleave', function() { this.style.transform = 'translateY(0) scale(1)'; });
                    btn.addEventListener('mousedown', function() { this.style.transform = 'translateY(-1px) scale(0.98)'; });
                    btn.addEventListener('mouseup', function() { this.style.transform = 'translateY(-3px) scale(1.02)'; });
                });
            }, 11000); // Delay execution until after vault sequence is complete

            document.addEventListener('mousemove', (e) => {
                if(document.body.style.overflow === 'auto') { // Only run parallax after vault is gone
                    const mouseX = e.clientX / window.innerWidth;
                    const mouseY = e.clientY / window.innerHeight;
                    document.querySelectorAll('.floating-3d').forEach((element, index) => {
                        const speed = (index + 1) * 0.7;
                        const x = (mouseX * speed * 30) - (speed * 15);
                        const y = (mouseY * speed * 30) - (speed * 15);
                        element.style.setProperty('--parallax-x', `${x}px`);
                        element.style.setProperty('--parallax-y', `${y}px`);
                        element.style.transform = `translate(var(--parallax-x, 0), var(--parallax-y, 0))`;
                    });
                }
            });
        });
    </script>
</body>
</html>